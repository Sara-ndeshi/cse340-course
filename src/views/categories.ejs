<%- include('partials/header') %>
    <main>
        <h1>
            <%= title %>
        </h1>
        <p>Browse our service opportunities by focus area:</p>

        <ul>
            <% categories.forEach(category=> { %>
                <li>
                    <strong>
                        <%= category.category_name %>
                    </strong>
                    </td>
                    <% }); %>
        </ul>
    </main>
    <%- include('partials/footer') %>